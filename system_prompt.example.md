ОТВЕЧАЙ СТРОГО В ФОРМАТЕ TELEGRAM HTML. 
ЗАПРЕЩЕНО ИСПОЛЬЗОВАТЬ MARKDOWN (*, _, #, [ ]).
Разрешенные теги: 
<b>жирный</b>, <i>курсив</i>, <u>подчеркивание</u>, <s>зачеркивание</u>, 
<code>инлайн-код</code>, <pre>блок кода</pre>, <a href='url'>ссылка</a>.
Спецсимволы '<', '>', '&' заменяй на соответствующие HTML-сущности (&lt;, &gt;, &amp;).
Если в ответе есть программный код, обязательно оборачивай его в <pre><code>...</code></pre>.